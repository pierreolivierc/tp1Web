{# Template pour l'index #}
{% extends "base.jinja" %}

{% block contenu %}
    <div class="p-2 d-flex">
    {% for i in objets_recuperer %}
        <div class="card border border-warning align-items-stretch mx-2" style="width: 18rem;">
            <img src="{{ src }}{{ i.photo }}" class="card-img-top" alt="{{ alt_image_article }}">
            <div class="card-body">
                <h2 class="card-title">
                    {{ i.titre }}
                </h2>
                <h4 class="card-subtitle mb-2 text-muted">
                    {{ i.categorie_description }}
                </h4>
                <p class="card-text">
                    {{ i.description }}
                </p>
                <form action="/details" method="POST">
                    <input type="hidden" name="objet_choisi" value="{{ i.id }}"/>
                    <button type="submit" class="btn btn-primary">Détails</button>
                </form>
            </div>
        </div>
    {% endfor %}
    </div>

{% endblock %}