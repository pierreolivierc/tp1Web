{% extends "base.jinja" %}

{% block contenu %}
    <div>
        <img src="{{ src_image_article }}" alt="{{ alt_image_article }}">
        <div>
            <h3>
                {{ titre_h3 }}
            </h3>
            <h4>
                {{ sous_titre }}
            </h4>
            <p>
                {{ message }}
            </p>
            <a href="{{ modifier }}" class="btn btn-primary">Modifier</a>
        </div>
    </div>
{% endblock %}